.card__content
  .card__content__info
    .card__content__info__image
      = image_tag(@product.images[0].image.url, size: '100x100')
    .card__content__info__message
      %h2 購入を確定しますか？
    .card__content__info__item
      %h2
      商品名
      = @product.name
    .card__content__info__price
      %h2
      ¥
      = @product.price
    .card__content__info__pay
      %h3 支払い方法
      - if @default_card_information.blank?
      - else
      .card__content__info__pay__number
        = "**** **** **** " + @default_card_information.last4
        - exp_month = @default_card_information.exp_month.to_s
        - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
        = exp_month + " / " + exp_year
    .card__content__info__button
      = form_tag(action: :pay, method: :post) do
        %button 購入する