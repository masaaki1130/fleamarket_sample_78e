.header
  .headerIn
    .headermain
      %h1.headermain__icon
        = link_to root_path do
          = image_tag ("logo.png"),class: "logoimage"
      .searchbar
        = form_with url: search_products_path, local: true, method: :get, class: "form" do |f|
          = f.text_field :keyword, placeholder: "キーワードから探す", class: "searchbar-input"
          = button_tag type: 'submit', class: 'search-btn' do
            .searchbar__icon
              = image_tag ("icon-search 1.png"), class: "searchicon"
    .tag
      %ul.lefttag
        %li.lefttag__item.lefttag__item--first
          = link_to "カテゴリ", "#", class: "post", id: "category__search search__btn" 
          %ul.category__parent.category__list
            - @parent.each do |parent|
              %li
                = link_to "#", class: "category__parent--name" do
                  = parent.name

        %li.lefttag__item
          = link_to "ブランド", "#", class: "post"
      - if user_signed_in?
        %ul.righttag
          %li.righttag__item.righttag__item--login
            = link_to "ログアウト", destroy_user_session_path, method: :delete, class: "post"
          %li.righttag__item.righttag__item--new
            = link_to "マイページ", mypage_index_path, class: "post"
      - else
        %ul.righttag
          %li.righttag__item
            = link_to "ログイン", new_user_session_path, class: 'post'
          %li.righttag__item
            = link_to "新規会員登録", new_user_registration_path, class: 'post'